\ The sibling of aot_collision_guard_dictionary_shadow.1z, for the other collision kind: a
\ definition landing on a same-scope import. The image restores the entry file's imports marked
\ `imported`, so the guard sees at binary runtime what it sees interpreted.
use { taken: } "fixtures/aot_collision_guard_mod.1z" ;

[ taken . ] call

\ The trailing word defers the definition to binary runtime in the durable entry frame.
taken: ( -- n ) [ 1 ] ; 0 drop
