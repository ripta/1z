\ Test: ONEZ_INTERPRETER_FALLBACK=0 disables fallback at runtime.
\ The word `keep` has a row-variable quotation parameter, so it
\ falls back to jitCallQuotation.  With fallback disabled, this
\ should crash with a diagnostic.
1 [ 2 + ] keep
. .
