\ The interpreter half of tests/aot/mixed_compare_locked_miss.1z. A locked AOT
\ binary reports a dispatch miss at a comparison site through its own trap, and
\ the two stderr goldens must differ only in the source path.

pick-num: ( b -- n ) [ [ 7 ] [ "x" ] if ] ;

f pick-num 3 < drop
