\ Test that --check gracefully handles recursive words without declared stack effects
\ (no crash, no false positive)

\ Recursive word with no stack effect -- should produce no diagnostic
count-down: [ dup 0 > [ 1 - count-down ] when ] ;
