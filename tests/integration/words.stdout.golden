Stack: [ ]
=== Native Primitives ===
#append ( seq1 seq2 -- seq )
#append! ( vec seq -- vec )
#drop ( seq n -- seq' )
#each ( seq quot: ( elem --  ) --  )
#empty? ( seq -- ? )
#ends-with? ( seq suffix -- ? )
#filter ( seq quot: ( elem -- ? ) -- seq' )
#first ( seq -- elem )
#in? ( seq elem -- ? )
#index-of ( seq elem -- n/f )
#last ( seq -- elem )
#len ( seq -- n )
#map ( seq quot: ( elem -- elem' ) -- seq' )
#nth ( seq n -- elem )
#pop ( seq -- seq' elem )
#pop! ( vec -- vec elem )
#prepend ( seq1 seq2 -- seq )
#push ( seq elem -- seq' )
#push! ( vec elem -- vec )
#reduce ( seq init quot: ( acc elem -- acc' ) -- value )
#shift ( seq -- seq' elem )
#shift! ( vec -- vec elem )
#slice ( seq start end -- subseq )
#starts-with? ( seq prefix -- ? )
#take ( seq n -- seq' )
#unshift ( seq elem -- seq' )
#unshift! ( vec elem -- vec )
% ( a b -- a%b )
(=) ( a b -- ? )
(trampoline) ( *unsafe-fn-ptr* --  )
* ( a b -- a*b )
*% ( a b -- a*b )
+ ( a b -- a+b )
+% ( a b -- a+b )
- ( a b -- a-b )
-% ( a b -- a-b )
/ ( a b -- a/b )
1array ( elem -- array )
; ( name quot --  )
< ( a b -- ? )
<channel> (  -- ch )
= ( a b -- ? )
> ( a b -- ? )
>bytes ( string -- byte-array )
>char ( codepoint -- str )
>codepoint ( str -- int )
>string ( value -- string )
>symbol ( string -- symbol )
>word ( name -- quotation )
@adjoin ( set value -- set' )
@difference ( set1 set2 -- set' )
@get ( assoc key -- value )
@get-or ( assoc key default -- value )
@has? ( assoc key -- ? )
@in? ( set value -- ? )
@intersection ( set1 set2 -- set' )
@keys ( assoc -- array )
@remove ( set value -- set' )
@remove! ( mmap key -- mmap )
@set ( assoc key value -- assoc' )
@set! ( mmap key value -- mmap )
@union ( set1 set2 -- set' )
@values ( assoc -- array )
add-load-path ( path --  )
await ( task -- value )
await-all ( array -- array )
benchmark ( quot -- hash )
benchmark-auto ( report label quot -- report )
benchmark-n ( report label n quot -- report )
benchmark-run ( report label quot -- report )
buffering-mode ( stream -- symbol )
bytes> ( byte-array -- string )
call ( quot --  )
cancel-task ( task --  )
cleanup ( body-quot cleanup-quot --  )
clock-monotonic (  -- sec nsec )
clock-realtime (  -- sec nsec )
command-line-args (  -- args )
compose ( quot1 quot2 -- quot' )
const (  -- marker )
curry ( x quot -- quot' )
define-enum ( name descriptor markers --  )
define-method ( name descriptor markers --  )
define-struct ( name descriptor markers --  )
define-virtual ( name descriptor markers --  )
dip ( x quot -- x )
drop ( a --  )
dup ( a -- a a )
enum-of ( val -- str/f )
enum-variants ( symbol -- array )
environ (  -- hash )
f (  -- f )
fd->stream ( int mode -- stream )
generic (  -- marker )
get ( param -- value )
help ( name --  )
if ( ? true-quot false-quot --  )
import ( module --  )
interpolate ( source template -- string )
load ( filename -- module )
lowercase ( str -- str )
make-benchmark-report (  -- report )
make-byte-array ( quotation -- byte-array )
make-error ( data message type -- error )
make-hash ( quotation -- hash )
make-mutable-map ( quotation -- mmap )
make-parameter ( name quot -- param )
make-set ( quotation -- set )
make-vector ( quotation -- vector )
marker (  -- marker )
match ( val branches -- ... )
mutable (  -- marker )
native? ( name -- ? )
over ( a b -- a b a )
parse-literal (  -- value )
parse-time (  -- marker )
parse-token (  -- string )
parse-tokens-until ( delimiter -- array )
parse-until ( delimiter -- quotation )
parse-values-until ( delimiter -- array )
pick-n ( n -- val )
print-benchmark-report
receive ( ch -- val )
recover ( try-quot recover-quot: ( error --  ) --  )
rethrow ( error --  )
send ( val ch --  )
set-buffering-mode ( stream symbol --  )
sleep ( duration --  )
spawn ( quot -- task )
spawn-named ( quot name -- task )
stderr (  -- stream )
stdin (  -- stream )
stdout (  -- stream )
stream->fd ( stream -- int )
stream-close ( stream --  )
stream-flush ( stream --  )
stream-open ( path mode -- stream )
stream-read ( stream n -- bytes )
stream-read-all ( stream -- bytes )
stream-read-line ( stream -- str/f )
stream-seek ( stream pos --  )
stream-seek-end ( stream offset --  )
stream-tell ( stream -- pos )
stream-write ( stream bytes -- n )
swap ( a b -- b a )
sys-exit ( code --  )
sys-info (  -- hash )
t (  -- t )
task-scope ( quot --  )
task-self (  -- task )
template ( string -- template )
throw ( error --  )
to-string ( value -- string )
type-of ( val -- symbol )
tz-decompose ( sec tz-name -- year month-num day hour min sec wday yday gmtoff tz-abbrev )
uppercase ( str -- str )
wipe ( ... --  )
with-parameter ( value param quot --  )
with-timeout ( quot duration -- value )
word-markers ( name -- markers )
words (  --  )
yield (  --  )

=== User-Defined Words ===
#all ( seq quot -- ? )
#any ( seq quot -- ? )
#first? ( seq -- option )
#last? ( seq -- option )
#none ( seq quot -- ? )
#nth? ( seq n -- option )
++ ( a1 -- a2 )
-- ( a1 -- a2 )
-@ ( a -- -a )
-rot> ( a b c -- c a b )
. ( a --  )
/= ( a b -- ? )
0? ( a -- ? )
1check ( a quot -- a )
2dip ( a b quot -- a b )
2drop ( a b --  )
2dup ( a b -- a b a b )
2keep ( ..a x y quot: ( ..a x y -- ..b ) -- ..b x y )
2nip ( a b c -- c )
3dip ( a b c quot -- a b c )
3drop ( a b c --  )
3dup ( a b c -- a b c a b c )
3keep ( ..a x y z quot: ( ..a x y z -- ..b ) -- ..b x y z )
<= ( a b -- ? )
<rot- ( a b c -- b c a )
>= ( a b -- ? )
>option:some
>result:err
>result:ok
?and-then ( opt/res quot -- opt/res )
?flatten ( nested -- opt/res )
?map ( opt/res quot -- opt/res )
?or-else ( opt/res quot -- opt/res )
@get? ( assoc key -- option )
B{
ECannotRedefineConst
EDivisionByZero
EFileNotFound
EFileReadFailed
EIOFailed
EIntegerOverflow
EKeyNotFound
ENoMatchingBranch
EStackEffectMismatch
EStackUnderflow
ETypeMismatch
EUnknownWord
EUserRethrown
EUserThrown
H{
M{
S{
V{
W{
[]
abs ( a -- b )
and ( ? ? -- ? )
array? ( val -- ? )
benchmark-report? ( val -- ? )
bi ( x quot1 quot2 --  )
bl (  --  )
boolean? ( val -- ? )
byte-array? ( val -- ? )
case ( val branches -- ... )
choose ( a1 a2 quot -- a )
cond ( branches -- ... )
do ( pred body -- pred body )
dupd ( a b -- a a b )
emit ( codepoint --  )
enum{
error-type? ( error type -- error ? )
error? ( val -- ? )
flush (  --  )
fmt ( source string -- string )
fmt-line ( source string --  )
generic? ( name -- ? )
hash? ( val -- ? )
ignore-errors ( quot --  )
input-stream
integer? ( val -- ? )
keep ( ..a x quot: ( ..a x -- ..b ) -- ..b x )
loop ( ... pred: ( ... -- ... ? ) -- ... )
marker? ( val -- ? )
max ( a1 a2 -- a )
method{
min ( a1 a2 -- a )
module? ( val -- ? )
mutable-map? ( val -- ? )
my-test-word
nip ( a b -- b )
nl (  --  )
no-matching-branch ( data -- error )
not ( ? -- ? )
option:none
option:none?
option:some>
option:some?
option?
or ( ? ? -- ? )
output-stream
parameter
parameter? ( val -- ? )
parse-quotation (  -- quotation )
pick ( a b c -- a b c a )
print ( str --  )
print-line ( str --  )
quotation? ( val -- ? )
read-line (  -- str/f )
rem ( a b -- r )
result:err>
result:err?
result:ok>
result:ok?
result?
run-benchmarks ( quot --  )
set? ( val -- ? )
stream-write-string ( stream str -- n )
stream? ( val -- ? )
string? ( val -- ? )
struct{
symbol? ( val -- ? )
times ( n quot --  )
try ( quot -- result )
tuck ( a b -- b a b )
unless ( ? quot --  )
unwrap-or ( opt/res default -- value )
unwrap-or-throw ( opt/res -- value )
use
vector? ( val -- ? )
virtual{
when ( ? quot --  )
while ( ..a pred: ( ..a -- ..b ? ) body: ( ..b -- ..a ) -- ..b )
with-input-stream ( stream quot --  )
with-output-stream ( stream quot --  )
xor ( ? ? -- ? )
Stack: [ ]
