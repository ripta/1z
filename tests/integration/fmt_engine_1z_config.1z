\ Placeholder: argv-mode test runs `1z fmt --engine=1z --stdout` over a fixture
\ governed by a `.fmt.1z`. Only the 1z engine reads that file, so the eight-space
\ indent in the golden is what proves the flag selected it. This file is not executed.
