\ Startup file fixture: a runtime fault mid-file. The word above it is defined, the word below it is
\ not, because the fault abandons the rest of the file.
before-fault: ( -- s ) [ "defined before the fault" ] ;

1 0 /

after-fault: ( -- s ) [ "never defined" ] ;
