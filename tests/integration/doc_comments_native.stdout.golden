Duplicate top of stack.
dup ( a -- a a ) \native

Stack: [ ]
Add two numbers. Promotes to bignum on fixnum overflow, or to float if either operand is a float.
+ ( a b -- a+b ) [<marker:generic>] \native

  method{ bignum bignum }
  method{ bignum fixnum }
  method{ bignum float }
  method{ fixnum bignum }
  method{ fixnum fixnum }
  method{ fixnum float }
  method{ float bignum }
  method{ float fixnum }
  method{ float float }
Stack: [ ]
Conditional execution.
if ( ..a ? true-quot: ( ..a -- ..b ) false-quot: ( ..a -- ..b ) -- ..b ) [<marker:branch-combinator>] \native

Stack: [ ]
Get length of sequence. O(1) and non-destructive; does not work on iterators (use #count instead).
#len ( seq -- n ) [<marker:generic>] \native

  method{ array }
  method{ byte-array }
  method{ hash }
  method{ module }
  method{ mutable-map }
  method{ set }
  method{ string }
  method{ vector }
Stack: [ ]
Execute try quotation; if error, run recover quotation with error on stack.
recover ( try-quot recover-quot: ( error -- ..a ) -- ) \native

Stack: [ ]
Open a file stream with mode (read:, write:, append:, read-write:).
stream-open ( path mode -- stream ) \native

Stack: [ ]
Spawn a new task from a quotation. The task is pinned to the worker with the fewest active tasks at spawn time for its entire lifetime. The returned handle is valid only within the task-scope that spawned it. After that scope exits the handle must not be used, because the runtime may have destroyed the task.
spawn ( quot -- task ) \native

Stack: [ ]
Create a dynamic parameter with a name and default quotation.
make-parameter ( name quot -- param ) \native

Stack: [ ]
Return the type of a value as a first-class type value.
type-of ( val -- type ) \native

Stack: [ ]
Get value by key from an associative type.
@get ( assoc key -- value ) \native

  method{ error }
  method{ hash }
  method{ module }
  method{ mutable-map }
Stack: [ ]
