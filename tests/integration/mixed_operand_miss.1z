\ The interpreter half of tests/aot/mixed_operand_locked_miss.1z. A locked AOT
\ binary reports a dispatch miss at an arithmetic site through its own trap, and
\ the two stderr goldens must differ only in the source path.

pick-num: ( b -- n ) [ [ 7 ] [ "x" ] if ] ;

f pick-num 3 + drop
