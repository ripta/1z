\ Placeholder: argv-mode test, the expression is passed via .flags.
\ This .1z file is not executed. It pins that both keys the startup file sets are readable from the
\ expression `eval` runs, which sits one pragma frame above the base one the file wrote to.
