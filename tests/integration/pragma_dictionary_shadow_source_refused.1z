\ A source file cannot relax the dictionary-shadow guard. Letting it would ship a dialect to every
\ later reader of the file, which is the hazard the set-site rule closes.
pragma{ dictionary-shadow: "off" }
