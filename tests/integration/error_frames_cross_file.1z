\ A raise inside a prelude compound reached through non-tail user words, so one chain crosses
\ source files: the raising native and its prelude callers render src/prelude.1z rows above the
\ user-file rows. No flags: one golden binds the interpreted and eager suites, and the AOT
\ suite builds this file through a `.source` redirect and diffs the same golden. The boundary
\ twin pinning frame presence for the same program is error_frames_mixed_compiled_above.1z.

say: ( v -- ) [ print-line 0 drop ] ;
report: ( v -- ) [ say 0 drop ] ;

42 report
