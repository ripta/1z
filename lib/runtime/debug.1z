\ lib/debug.1z - Debugging utilities

scope-trace: ( -- ) [
  native.scope-frames
  >iterator [
    dup "type" @get
    over "index" @get dup -1 =
    [ drop ]
    [ >string "[" swap #append "]" #append #append ]
    if
    ": " #append
    over "count" @get >string #append " words" #append
    over "import-frame?" @get [ " (import-frame)" #append ] when
    nip
    eprint-line
  ] #each
] ;
