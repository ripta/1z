\ lib/ranges.1z - Ranging over integers

\ Helper for range: ( arr counter limit -- arr )
range-loop: [
  2dup <
  [
    [ swap over 1array #append swap ++ ] dip
    range-loop
  ]
  [ 2drop ]
  if
] ;

\ Generate exclusive range [start, end)
range: ( start end -- array ) [
  2dup <
  [
    { } -rot>      \ arr start end
    range-loop     \ arr
  ]
  [ 2drop { } ]
  if
] ;

\ Generate exclusive range [0, n)
iota: ( n -- array ) [ 0 swap range ] ;

\ Generate inclusive range [start, end]
range-inclusive: ( start end -- array ) [ ++ range ] ;
